合并两个数组 - concat()

合并三个数组 - concat()

用数组的元素组成字符串 - join()

删除数组的最后一个元素 - pop()  // 返回删除的元素

数组的末尾添加新的元素 - push() // 返回数组长度

将一个数组中的元素的顺序反转排序 - reverse()

删除数组的第一个元素 - shift()  // 返回删除的元素

从一个数组中选择元素 - slice()

数组排序（按字母顺序升序）- sort()

数字排序（按数字顺序升序）- sort()

数字排序（按数字顺序降序）- sort()

在数组的第2位置添加一个元素 - splice()

转换数组到字符串 -toString()

在数组的开头添加新元素 - unshift()  // 返回数组长度